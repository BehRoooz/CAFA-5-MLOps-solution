# Integrated stack: Embedding API + GO Prediction API + MLflow.
# Run from repo root: docker compose up --build
services:
  trainer-api:
    build:
      context: .
      dockerfile: docker/docker_training/Dockerfile.training
    image: cafa5-trainer-api:local
    working_dir: /app
    profiles: ["training"]
    environment:
      PYTHONUNBUFFERED: "1"
      PYTHONPATH: /app
      MLFLOW_TRACKING_URI: http://mlflow:5000
      # Host browser URL for run/registry links returned by GET /api/train/jobs/{id}
      MLFLOW_EXTERNAL_UI_BASE: http://127.0.0.1:5000
      REGISTERED_MODEL_NAME: cafa-go-model
      PROMOTION_THRESHOLD: "0.35"
    depends_on:
      - mlflow
    volumes:
      - ./data:/app/data
      - ./outputs:/app/outputs
      - ./mlruns:/mlruns
    ports:
      - "8002:8000"

  embedding-api:
    build:
      context: .
      dockerfile: docker/docker_embedding/Dockerfile.embedding-api
    image: cafa5-embedding-api:local
    working_dir: /app/services/embedding-api
    environment:
      PYTHONUNBUFFERED: "1"
      PYTHONPATH: /app:/app/services/embedding-api
      GO_PREDICTION_API_URL: http://go-prediction-api:8000
      MLFLOW_TRACKING_URI: http://mlflow:5000
    depends_on:
      - go-prediction-api
      - mlflow
    volumes:
      - ./outputs:/app/outputs
      - ./data/hf_cache:/app/data/hf_cache
    ports:
      - "8000:8000"

  go-prediction-api:
    build:
      context: .
      dockerfile: docker/docker_go_term/Dockerfile.api
    image: cafa5-go-prediction-api:local
    working_dir: /app/services/go-prediction-api
    environment:
      PYTHONUNBUFFERED: "1"
      PYTHONPATH: /app:/app/services/go-prediction-api
      MLFLOW_TRACKING_URI: http://mlflow:5000
      MODEL_URI: models:/cafa-go-model@champion
      MODEL_CACHE_DIR: /app/model_cache
    depends_on:
      - mlflow
    ports:
      - "8001:8000"
    volumes:
      - ./outputs:/app/outputs

  mlflow:
    image: ghcr.io/mlflow/mlflow:v2.13.0
    # Pin store + artifacts to the mounted volume (otherwise the server may use a different cwd ./mlruns).
    command: >
      mlflow server --host 0.0.0.0 --port 5000
      --backend-store-uri file:///mlruns
      --default-artifact-root file:///mlruns
    volumes:
      - ./mlruns:/mlruns
    ports:
      - "5000:5000"